# Binaries
/clio
/dist/
*.exe

# SQLite (clio's own DB should never be committed)
*.sqlite
*.sqlite-wal
*.sqlite-shm

# Claude Code per-machine local settings (shared settings.json is tracked)
.claude/settings.local.json

# OS
.DS_Store

# Local AI-tool session state (codex consult continuity)
.context/
