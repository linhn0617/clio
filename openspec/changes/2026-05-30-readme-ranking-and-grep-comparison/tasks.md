## 1. Update the READMEs

- [x] 1.1 In `README.md`, change the `search` MCP-tools row to note recency-aware
  ranked search, and add a "Why not just `grep`?" section before "Getting started".
- [x] 1.2 Mirror both edits in `README_zh-TW.md` (Traditional Chinese), keeping the
  bilingual pair in sync.

## 2. Verify

- [x] 2.1 Both READMEs render correctly (headings, table, bullet list) and the new
  section reads naturally in each language.
- [x] 2.2 No code or behavior change — the only product change is the two README
  files; the rest of the diff is this openspec change (proposal, tasks, doc spec).
- [x] 2.3 Third-party (codex) review of the actual diff passes (round 2: P1 resolved,
  one P2 on proposal wording fixed; gate PASS).
