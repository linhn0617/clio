## 1. `<private>` stripping at ingest (spec: session-ingest)

- [x] 1.1 Add `stripPrivate` to `internal/ingest/redact.go` (`(?is)<private(?:\s[^>]*[^/>])?\s*>.*?</private\s*>` → empty; self-closing `<private/>` is not an opener) and call it as the first statement of `redactString`, before the `{`/`[` split loop, and also in `redactJSON`'s decode-failure fallback (design D8); `toolUseSummary` in `parser.go` redacts before its 200-byte truncation (as `codex.go`'s tool summary already does); confirm by grep that every persisted text path (`ParseLine` content/titles, `toolUseSummary`, `extractTargets`, `redactWalkDepth` string leaves) reaches `redactString`
- [x] 1.2 Table tests in `internal/ingest/redact_test.go`: single block, two blocks in one message, unterminated opener left intact, case-insensitive tag, tag with attributes, block spanning newlines, **private block whose content is a JSON object/array** (`<private>{"customer":"Acme"}</private>` fully removed via `redactString`), self-closing `<private/>` (bare and with attributes) is not an opener, closer with trailing whitespace, nested blocks removed as one element (depth-tracking scan; the earlier non-greedy regex leaked the outer tail — found by the final verifier), secret inside a private block leaves no `[REDACTED:…]` marker
- [x] 1.3 Parser-level test in `internal/ingest/parser_test.go`: a user event whose first text block opens with `<private>…</private>` followed by real text yields a title from the real text and a `raw_json` without the private span; a tool_result string leaf containing the tag is stripped too; a >200-byte private block in a tool_use `command` leaves nothing in the summary; a span across a tool_result's nested blocks is stripped from content but kept in raw_json (pinned); a Codex transcript is stripped too (`codex_test.go`); a block that opens in one content block and closes in the next is left as-is in both (documented non-support)
- [x] 1.4 Retrieval check in `internal/eval` (or `ingest_test.go` against a temp DB): after ingesting a fixture with `<private>Acme</private>`, `search.Search("Acme")` returns no hit

## 2. Recall tail excerpt (spec: recall-hook, cli-surface)

- [x] 2.1 Add `TailExcerpt{SessionUUID, TS, Text}` and `Recall.LastAssistant *TailExcerpt` in `internal/sessions/sessions.go`; extend `GetRecall`: excerpt from the digest's first listed session, falling back to a since-unbounded `ListSessions` (same page size) when the window is empty, then the last non-blank `role='assistant'` row by `seq DESC` (design D7; undated row → session `ended_at`); tests pin `ended_at DESC`, `uuid DESC` tiebreak, orphan promotion, the since fallback, digest-row agreement, and blank-row skipping
- [x] 2.2 Tests in `internal/sessions/sessions_test.go`: newest session's last assistant text is returned; a trailing `tool_use` row is skipped in favour of the earlier `assistant` row; a session with no assistant text yields `nil`; a subagent child whose parent is present and in range is not chosen over the parent; a promoted orphan subagent (parent absent) *is* chosen, matching `ListSessions`
- [x] 2.3 `internal/cli/recall.go`: `--tail-runes` flag (default 600, `0` omits); `formatRecall` prints the "Last session left off (date, id prefix):" section first, whitespace-collapsed, cut on a rune boundary with `…`
- [x] 2.4 Tests in `internal/cli/recall_test.go`: section present with truncation at the bound, section absent when `LastAssistant == nil`, section absent with `--tail-runes 0`, existing `TestFormatRecall*` still pass unchanged

## 3. `clio file-history` query and CLI (spec: file-history, cli-surface)

- [x] 3.1 Add `FileHistory(ctx, db, key, excludeSession string, since int64, limit int) ([]FileTouch, newestTS int64, error)` in `internal/sessions/` implementing the join in design D2 (kind/value index, parent roll-up via COALESCE, `db.SourceFilter` default source, current-session exclusion, `GROUP_CONCAT(DISTINCT tool_name)`, `NULLS LAST`); `newestTS` is the separate `MAX(tt.ts)` over the session-joined, source-filtered rows for the path with **no** LIMIT and **no** caller exclusion (D2); expose `ingest.NormalizeTargetPath` = `capValue(redactString(abs))` and use it for the lookup key (D3)
- [x] 3.2 Tests in `internal/sessions/sessions_test.go`: two editing sessions + one reading session ordered newest first with the right tool sets; subagent touch rolls up to its parent; Codex session excluded by default; `excludeSession` drops the caller from rows but not from `newestTS`; `since` bounds; `newestTS` ignores LIMIT; an orphan `tool_targets` row with no `sessions` row neither appears nor moves `newestTS`; a NULL `ts` row sorts last and prints `(undated)`
- [x] 3.3 New `internal/cli/file_history.go`: `clio file-history <path> [--limit 10] [--since]`; `filepath.Abs` then `ingest.NormalizeTargetPath` on the argument (design D3); read-only DB open; every error → empty stdout, exit 0 (mirror `recallDigest`); register in `internal/cli/root.go`; test that `clio --help` lists `file-history`
- [x] 3.4 Plain-text formatter + tests in `internal/cli/file_history_test.go`: row shape `date  title  (id prefix)  tools`, newest first, empty result prints nothing, trailing pointer line to `clio show` / `read_session`; CLI-mode error paths (missing index dir, unreadable DB file) print nothing and return nil; a relative argument and its absolute spelling produce identical output

## 4. PreToolUse hook mode and installer (spec: file-history, recall-hook)

- [x] 4.1 `--hook` mode in `file_history.go`: parse stdin JSON (`tool_name`, `tool_input.file_path`, `session_id`; `cwd` is not needed because `file_path` is absolute; reuse `readStdinIfPiped`, 64 KB cap); `Args` accepts no positional argument in hook mode; bail silently unless `tool_name == "Read"` and `file_path` is present; apply the staleness gate `os.Stat(path).ModTime().Unix() > newestTS` → empty, stat failure → gate skipped (design D4); cap the digest at 8,000 characters with a `(+N more)` line (D5); on content, print `{"hookSpecificOutput":{"hookEventName":"PreToolUse","additionalContext":…}}` with no `permissionDecision`
- [x] 4.2 Tests in `file_history_test.go`: Read payload with fresh history → JSON with the digest; file mtime newer than `newestTS` → empty; file missing on disk → digest still printed; `tool_name: "Edit"` → empty; empty / non-JSON / missing `file_path` stdin → empty; 200 long rows → output ≤ 8,000 chars with a `(+N more)` line before the pointer line; a path so long that no row fits → hook silent (`TestFileHistoryHookSilentWhenNothingFits`); every case asserted on `fileHistoryHook`'s return (cobra's `RunE` only prints it and returns nil); exit 0 / empty stdout+stderr verified on the built binary in 5.6
- [x] 4.3 Generalise `internal/claudeconfig/hooks.go` per design D6: shared `removeHookEntriesIn`/`hasHookEntry`/`groupsHave(WithMatcher)` helpers parameterised by recogniser; `AddClioHooks`/`RemoveClioHooks` do both hooks in one `mutate` (so `.bak` is the pre-install file); `AddPreToolUseReadHook`/`RemovePreToolUseReadHook`/`HasPreToolUseReadHook` per-hook wrappers; PreToolUse entries carry `"matcher":"Read"` and `"timeout":10`, presence counts only inside a `Read` group; keep `isClioRecall` semantics byte-for-byte and add `isClioFileHistory` (`<…/clio> file-history --hook`)
- [x] 4.4 Tests in `internal/claudeconfig/hooks_test.go`: PreToolUse group added with matcher and timeout; existing PreToolUse hooks (e.g. a user's own `Read` gate) preserved; entry under another matcher does not count as installed; empty `fileHistoryCmd` removes an existing hook; re-run on a SessionStart-only config adds only PreToolUse (`install_hook_test.go`); remove drops only clio's entries from both events and deletes an emptied event key; `clio-helper file-history --hook` does not match; all existing `TestAddSessionStartHook*` / `TestRemoveSessionStartHook*` still pass
- [x] 4.5 `internal/cli/install_hook.go`: `--no-file-context` flag (registers recall only and removes a previously installed file-history hook); `uninstall-hook` removes both and drops an emptied `hooks` object; messages name what was actually added/removed (re-run says nothing to add); tests in `install_hook_test.go` for the flag, for the re-run-adds-only-PreToolUse path, and (extending `TestInstallHookPreservesExistingConfig`) that `~/.claude.json`'s `mcpServers` is byte-identical before and after `install-hook`

## 5. Docs, changelog, verification

- [x] 5.1 README.md + README_zh-TW.md: `file-history` in the command table; hook section rewritten for two hooks, `--no-file-context`, and the re-run note; new "Privacy: `<private>`" paragraph stating clio does not index the span, Claude Code still saw it, history needs `clio index --full`, source JSONL is never edited; `recall` line mentions the last-session excerpt and states it is the previous session's raw closing text (not a summary; may quote external content the session was reading), off with `--tail-runes 0`
- [x] 5.2 docs/USAGE.md (bilingual): same three additions with examples of the plain and `--hook` output
- [x] 5.3 CHANGELOG.md `[Unreleased]`: Added `file-history` + PreToolUse hook; Changed `recall` digest and `install-hook`; Added `<private>` stripping with the re-index note
- [x] 5.4 `go build ./... && go vet ./... && go test ./...` clean; `gofmt -l` empty
- [x] 5.5 Latency record (design D5 method) appended to design.md: 3 warm runs of `clio file-history --hook` with a real payload on the reference machine, plus one cold run; confirm all under the 10 s hook timeout and warm runs ≲ 100 ms
- [ ] 5.6 Manual smoke in a real Claude Code session: `clio install-hook`, restart, Read a file with history → the timeline appears as a system reminder before the file content; Read a file without history → nothing injected; `clio uninstall-hook` → `~/.claude/settings.json` has no clio entries left; paste `<private>x</private>` in a prompt, wait for the watcher, `clio search x` → no hit
- [x] 5.7 Fresh-context verifier pass over specs vs implementation (each spec scenario mapped to a test name or a smoke step), then `/codex` cross-model review of the diff per repo convention
