## 1. Documentation

- [x] 1.1 README (EN + zh-TW): MCP tools table adds `ask` and refreshes
  `list_sessions` / `activity_summary`; CLI examples add `clio ask` /
  `clio activity` / `clio recall`; note the opt-in `clio install-hook`.
- [x] 1.2 `docs/USAGE.md`: add `clio ask` and `clio activity` sections (incl. the
  `--touched`/`--tool`/`--ran` filters), add the `ask` MCP tool, and add
  `recall` / `install-hook` / `uninstall-hook` to Other commands + the cheat sheet.
- [x] 1.3 `openspec validate --strict`; the English and zh-TW READMEs stay in sync.
